# Environment variables (secrets)
.env
*.env.local

# LLM API key fallback file (used by simulation/llm_client.py)
llm_api.key

# Opencode personal configuration
.agents/
skills-lock.json

# Mist deploy configuration (contains VM credentials)
mist_deploy.conf

# AI assistant instructions (personal, not for repo)
AGENTS.md

# Edge deploy configuration (contains VM/Jetson credentials)
edge_deploy.conf

# Python virtual environments
venv/
