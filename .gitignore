# Environment variables (secrets)
.env
*.env.local

# LLM API key fallback file (used by simulation/llm_client.py)
llm_api.key

# Opencode personal configuration
.agents/
skills-lock.json

# Per-tier deploy configurations (contain Tailscale IPs / SSH credentials)
# Matches mist_deploy.conf, edge_deploy.conf, fog_deploy.conf, cloud_deploy.conf,
# and any future <tier>_deploy.conf. The matching *.conf.example is committed.
*_deploy.conf

# Cloud-tier model comparison artefacts (local-only, not published)
multi-tier-deployment/cloud_deploy/model_comparison/

# AI assistant instructions (personal, not for repo)
AGENTS.md
**/AGENTS.md

# Python virtual environments
venv/
